{
  "name": "website",
  "version": "1.0.0",
  "description": "Example of a website with:",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon dev-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen-paths": "node tools/generate-regexp-paths"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.3",
    "path-to-regexp": "^6.1.0"
  }
}
